%section
  %header.major
    %h2= @article.title
  %p
    %span.image.left
      %img{ src: @article.image.url(:landscape) }
    = @article.text.gsub("\r\n", "</p><p>").html_safe

%section
  = setting(:comments).html_safe